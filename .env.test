# Test environment configuration
# This file is used for testing only and should be committed to version control

# API Keys (Required for tests - use dummy values)
DEEPSEEK_API_KEY=test-deepseek-key

# Optional API Keys (not needed for unit tests)
GOOGLE_API_KEY=
PUSHOVER_USER=
PUSHOVER_TOKEN=

# Agent Configuration
AGENT_NAME=You Name
PRIMARY_MODEL=deepseek-chat
EVALUATOR_MODEL=gemini-2.0-flash

# Feature Flags
ENABLE_RAG=true
ENABLE_EVALUATION=true
ENABLE_MEMORY=false

# Paths
DB_PATH=data/test_career_qa.db
CHROMA_PATH=data/test_chroma_db
KB_PATH=knowledge_base
